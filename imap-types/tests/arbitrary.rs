#[cfg(feature = "arbitrary")]
#[test]
fn arbitrary() {
    // use arbitrary::{Arbitrary, Unstructured};
    // use imap_types::command::Command;
    //
    // let data = include_bytes!("../crash.bin");
    //
    // let mut u = Unstructured::new(data);
    //
    // Command::arbitrary(&mut u).unwrap();
}
