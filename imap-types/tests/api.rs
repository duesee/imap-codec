#[test]
fn construction_of_atom() {
    // `inner` is a private field
    // let atm = Atom {
    //     inner: Cow::Borrowed(" x "),
    // };

    // let mut atm = Atom::try_from("valid").unwrap();

    // `inner` is a private field
    // atm.inner = Cow::Borrowed(" x x x ");

    // Panics
    // let mut atm = Atom::try_from(" x ").unwrap();

    // Requires unsafe
    // let atm = unsafe { Atom::unchecked(" x "); }
}
